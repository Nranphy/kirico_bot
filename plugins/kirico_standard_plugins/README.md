# 标准插件目录

* * *

用于存放使用 `kirico` 标准编写或改写的插件。

- 本目录插件将被统一加载。

- 使用 `kirico` 标准意味着调用了 `kirico_bot` 所提供的 api，并不用于区分是否使用 `kirico_plugin_store` 所提供的插件模板。

- 一般而言，此处插件**不可**直接用于其他基于 `NoneBot2` 的机器人程序，需要先进行解耦操作。

- 在取用插件与改写插件时请注意不同插件的开源许可协议。